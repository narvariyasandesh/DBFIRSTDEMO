@model DBFIRSTDEMO.Models.Membership

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>


@using (Html.BeginForm("AddMembership", "Membership",FormMethod.Post)) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Membership</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group row">
            @Html.LabelFor(model => model.membertype, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.membertype, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.membertype, "", new { @class = "text-danger" })
            </div>
            <div class="col-md-offset-2 col-md-4">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<hr />
<div>
    <table class="table">
        <tr>
            <th>
                member types)
            </th>
            <th></th>
        </tr>

       
        @foreach (var item in ViewBag.member)
        {
            <tr>
                <td>
                    @item.membertype
                </td>
                <td>                   
                    @*@Html.ActionLink("Delete", "Delete", new { id = item.id })*@
                    <a href="@Url.Action("Delete",new { id = item.id})", class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }

    </table>

</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
